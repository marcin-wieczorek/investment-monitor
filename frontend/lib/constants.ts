/** An investment newer than this is badged as "NEW" in the UI. */
export const NEW_THRESHOLD_MS = 7 * 24 * 60 * 60 * 1000;

/** A source not captured within this window is flagged as stale/unhealthy. */
export const STALE_THRESHOLD_MS = 24 * 60 * 60 * 1000;
