# Path to the shared SQLite database written by the Kotlin monitoring
# pipeline. Relative paths are resolved from the frontend/ directory.
# Defaults to ../investment-monitor.db (repo root) if unset.
DATABASE_PATH=../investment-monitor.db
